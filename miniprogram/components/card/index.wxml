<!--components/card/index.wxml-->
<view class="card rows {{class1}}" style="{{style1}}">
  <view wx:if="{{tag_top}}" class="tag_top" style="{{style2}}">{{tag_top}}</view>
  <view style="padding: 1rem;">
    <slot> </slot>
  </view>
</view>